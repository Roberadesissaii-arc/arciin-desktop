{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "arciin-window-controls",
  "description": "Minimize, maximize and close for the Arciin window. Scoped to our own overlay webview by label, so the webview showing the server's page is still granted nothing at all.",
  "windows": ["arciin"],
  "webviews": ["arciin-chrome"],
  "permissions": [
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:event:default"
  ]
}
